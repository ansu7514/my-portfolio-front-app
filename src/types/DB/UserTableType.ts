export interface UserTableType {
    user_id: string,
    name?: string,
    email?: string,
    phone?: string,
    job?: string,
    birth?: string,
    address?: string,
    image?: string,
    image_path?: string,
}